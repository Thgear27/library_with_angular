import { BooksGridConfiguration } from './books-grid-configuration';

describe('BooksGridConfiguration', () => {
  it('should create an instance', () => {
    expect(new BooksGridConfiguration()).toBeTruthy();
  });
});
